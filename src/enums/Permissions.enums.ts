export enum Method {
    GET = "GET",
    POST = "POST",
    PUT = "PUT",
    DELETE = "DELETE"
}

export enum Scope {
    READ = "read",
    WRITE = "write",
    UPDATE = "update",
    DELETE = "delete"
}